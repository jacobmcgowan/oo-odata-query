/**
 * Defines an OData parameter.
 */
export interface IParameter {
  /**
   * Outputs the OData parameter string.
   * @returns {string} The resulting parameter string.
   */
  toString(): string;
}
